{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: todo-app
  labels:
#   annotations:
#     dev.okteto.com/generate-host: "todo-app"
spec:
  rules:
      - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: todo-app
                port:
                  number: {{ .Values.service.port }}
{{- end }}